---
// Contact Section
---

<section id="contact" class="contact-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Get In <span class="text-gradient">Touch</span></h2>
      <p class="section-subtitle">
        I'm always open to discussing new projects, opportunities, or collaborations
      </p>
    </div>

    <div class="contact-content">
      <div class="contact-info">
        <div class="info-card">
          <div class="info-icon">📧</div>
          <h3 class="info-title">Email</h3>
          <a href="mailto:briangray42@gmail.com" class="info-link">
            briangray42@gmail.com
          </a>
        </div>

        <div class="info-card">
          <div class="info-icon">📍</div>
          <h3 class="info-title">Location</h3>
          <p class="info-text">
            Greenbrier, Arkansas
          </p>
        </div>
      </div>

      <div class="contact-form-wrapper">
        <div class="contact-cta">
          <h3>Ready to collaborate?</h3>
          <p>
            Feel free to reach out via email. I'm always interested in hearing about
            new projects, opportunities, and ways to collaborate.
          </p>
          <a href="mailto:briangray42@gmail.com" class="btn btn-primary btn-large">
            Send Me an Email
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-section {
    background: var(--color-bg-alt);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }

  .section-title {
    margin-bottom: var(--space-md);
  }

  .section-subtitle {
    font-size: 1.125rem;
    color: var(--color-text-light);
    max-width: 600px;
    margin: 0 auto;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    max-width: 1000px;
    margin: 0 auto;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .info-card {
    background: white;
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    transition: var(--transition);
  }

  .info-card:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-4px);
  }

  .info-icon {
    font-size: 2rem;
    margin-bottom: var(--space-sm);
  }

  .info-title {
    font-size: 1.125rem;
    color: var(--color-text-dark);
    margin-bottom: var(--space-sm);
  }

  .info-link {
    color: var(--color-primary);
    font-weight: 500;
    display: inline-block;
  }

  .info-link:hover {
    text-decoration: underline;
  }

  .info-text {
    color: var(--color-text);
    margin: 0;
    line-height: 1.7;
  }

  .social-links {
    background: white;
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
  }

  .social-title {
    font-size: 1.125rem;
    color: var(--color-text-dark);
    margin-bottom: var(--space-md);
  }

  .social-icons {
    display: flex;
    gap: var(--space-md);
  }

  .social-link {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-bg-alt);
    border-radius: var(--radius-md);
    color: var(--color-text);
    transition: var(--transition);
  }

  .social-link:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .contact-form-wrapper {
    display: flex;
    align-items: center;
  }

  .contact-cta {
    background: white;
    padding: var(--space-2xl);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    text-align: center;
  }

  .contact-cta h3 {
    font-size: 1.75rem;
    color: var(--color-text-dark);
    margin-bottom: var(--space-md);
  }

  .contact-cta p {
    color: var(--color-text);
    line-height: 1.7;
    margin-bottom: var(--space-xl);
  }

  .btn-large {
    padding: 1rem 2rem;
    font-size: 1.125rem;
  }

  @media (max-width: 768px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .social-icons {
      flex-wrap: wrap;
    }
  }
</style>
